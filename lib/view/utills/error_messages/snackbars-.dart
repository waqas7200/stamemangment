import 'package:get/get.dart';

class AppSnackBar
{
  static shoeErrorSnackbar(String title, String message)
  {
    Get.snackbar(title, message);
  }
  static shoeSuccessSnackbar(String title, String message)
  {
    Get.snackbar(title, message);
  }
  static shoeWarningSnackbar(String title, String message)
  {
    Get.snackbar(title, message);
  }
}